Steps:

Initialize counter

Start with count = 1 (since the first word always starts with a lowercase letter in camelCase).

Iterate through the string

For each character ch in s:

If ch is uppercase, increment count by 1.

End

After processing all characters, return count.