Explanation of Logic:

Sort weights so we can group toys optimally.

Start with the first toy and set a check limit (weight + 4).

For each toy, if its weight exceeds check, start a new container and update check.

Return the number of containers used.