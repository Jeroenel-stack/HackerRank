Steps:

Loop d times (number of rotations):

Remove the first element of the array: firstElement = arr.shift()

Append that element to the end of the array: arr.push(firstElement)

Repeat until all d rotations are performed

Return the rotated array