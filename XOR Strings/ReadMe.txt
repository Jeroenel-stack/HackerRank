Explanation:
Input: Two strings s and t of equal length, containing only '0' and '1'.
Logic: For each character in the strings:
If the characters are the same (s[i] === t[i]), append '0' to the result.
Otherwise, append '1'.
Output: A new string representing the XOR of the two input strings.