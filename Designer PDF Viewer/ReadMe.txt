Steps:

Map letters to heights

Create a map letterHeight.

For each letter 'a' to 'z', map it to its corresponding height in array h.

Find the tallest letter in the word

Split the word into characters.

For each character, look up its height in letterHeight.

Keep track of the maximum height maxHeight.

Calculate area

Multiply the maximum height maxHeight by the length of the word → area = maxHeight * word.length.

Return result

Return area.