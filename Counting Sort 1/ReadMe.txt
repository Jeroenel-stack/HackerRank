Steps:

Initialize frequency array

Create an empty array finalArr.

Fill it with 100 zeros (indices 0 to 99).

Count frequencies

For each element el in the input array arr:

Increment finalArr[el] by 1.

Return result

Return finalArr, which now contains the frequency of each integer from 0 to 99.