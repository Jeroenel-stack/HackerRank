Steps:

Loop through the array from index 0 to arr.length - 1

For each i:

Check if arr[i] === V

If true → return i (index of the target)

End of function

If the target is guaranteed to exist, the function will always return inside the loop.